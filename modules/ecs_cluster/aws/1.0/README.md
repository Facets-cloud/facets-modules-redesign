# AWS ECS Cluster

Creates an AWS ECS cluster with Fargate or Fargate Spot capacity. The AWS provider
is supplied by the `@facets/aws_cloud_account` input.

The module emits `@facets/ecs-cluster` with the cluster ARN and name.
