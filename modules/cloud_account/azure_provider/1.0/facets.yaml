clouds:
    - azure
composition: {}
conditional_on_intent: ""
depends_on: []
description: Module to configure and provision resources within a cloud account
flavor: azure_provider
iac:
    validated_files:
        - variables.tf
input_type: instance
intent: cloud_account
lifecycle: ENVIRONMENT
outputs:
    default:
        mock:
            attributes:
                client_id: mock-string-value
                client_secret: mock-string-value
                secrets: []
                subscription_id: mock-string-value
                tenant_id: mock-string-value
            interfaces: {}
        providers:
            azapi:
                attributes:
                    client_id: attributes.client_id
                    client_secret: attributes.client_secret
                    subscription_id: attributes.subscription_id
                    tenant_id: attributes.tenant_id
                source: Azure/azapi
                version: 2.5.0
            azurerm:
                attributes:
                    client_id: attributes.client_id
                    client_secret: attributes.client_secret
                    features: {}
                    subscription_id: attributes.subscription_id
                    tenant_id: attributes.tenant_id
                source: hashicorp/azurerm
                version: 4.36.0
        type: '@facets/azure_cloud_account'
sample:
    disabled: false
    flavor: azure_provider
    kind: cloud_account
    spec:
        cloud_account: example-azure-account
    version: "1.0"
spec:
    description: Module to configure and provision resources within a cloud account
    properties:
        cloud_account:
            description: Select the cloud account to use (must be previously linked)
            title: Cloud Account
            type: string
            x-ui-api-source:
                endpoint: /cc-ui/v1/accounts/
                filterConditions:
                    - field: accountType
                      value: CLOUD
                    - field: provider
                      value: AZURE
                labelKey: name
                method: GET
                params:
                    includeContent: false
                valueKey: id
            x-ui-overrides-only: true
            x-ui-show-label-selected: true
            x-ui-typeable: true
    required:
        - cloud_account
    title: Cloud Account Manager
    type: object
    x-ui-order:
        - cloud_provider
        - cloud_account
version: "1.0"
