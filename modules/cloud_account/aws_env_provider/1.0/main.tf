# AWS Environment Provider Module
# Credentials are auto-discovered from the pod environment (IAM role, IRSA, instance profile, etc.)
# No explicit credential configuration is needed - only the region is set.
